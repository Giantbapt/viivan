const button = document.querySelector('.click-me');
const Michelle = []

button.addEventListener('click', (event) => {
  <%= Michelle =  Galerie.search_by_tags("Michelle") %>;
  console.log(Michelle);
  console.log(event);
});

// app/views/races/index.js.erb

// function updateGaleries(galeriesHTML) {
//   const galeries = document.querySelector('.click-me');
//   galeries.insertAdjacentHTML('beforeend', galeriesHTML);

//   // const pages = document.querySelector('.pages');
//   // pages.innerHTML = pagesHTML;
// }

// updateGaleries(
//   '<%= j render "galeries", galeries: @galeries %>'
//   // '<%= j paginate(@races, remote: true).to_s %>'
// );
